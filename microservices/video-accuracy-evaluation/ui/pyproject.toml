[tool.poetry]
name = "video-accuracy evaluation tool ui"
version = "0.1"
description = "Gradio UI for video-accuracy evaluation tool"
authors = []

[tool.poetry.dependencies]
python = "^3.10"
requests = ">=2.32.5"
gradio = "5.49.0"

[build-system]
requires = ["poetry-core>=1.8.3"]
build-backend = "poetry.core.masonry.api"